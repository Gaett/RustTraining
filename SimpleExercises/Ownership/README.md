Simple test for DataTypes
Things I learned:
- See inline comments in main.rs
- After some exercise, I should probably stop explicitly writing the types cause it adds noises in the code. The compiler is smarter than me.

Commands I learned / used:
- `cargo init`
- `cargo run`